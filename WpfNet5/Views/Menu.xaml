<core:ContentPage x:Class="WpfNet5.Views.Menu"
            x:TypeArguments="vm:MenuViewModel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:core="clr-namespace:WpfNet5.Core;assembly=WpfNet5.Core"
      xmlns:vm="clr-namespace:WpfNet5.ViewModels"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfNet5"
      mc:Ignorable="d" 
      Background="White"
      d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Command="{Binding ShowFirst}">First view</Button>
        <Button Command="{Binding ShowSecond}" Grid.Row="1">Second view</Button>
        <Button Grid.Row="2" Command="{Binding ShowAdmin}">Admin view</Button>
        <Button Grid.Row="3" Command="{Binding ShowUsers}">Users view</Button>
    </Grid>

</core:ContentPage>
