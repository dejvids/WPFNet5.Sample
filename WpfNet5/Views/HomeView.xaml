<core:ContentPage x:Class="WpfNet5.Views.HomeView"
      x:TypeArguments="vm:HomeViewModel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:core="clr-namespace:WpfNet5.Core;assembly=WpfNet5.Core"
      xmlns:vm="clr-namespace:WpfNet5.ViewModels"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
            Background="#ECECEB">

    <Border BorderThickness="3" BorderBrush="#6C7A91">
        <Grid>
            <!--<ProgressBar Height="20" Width="100" Panel.ZIndex="100" Visibility="Collapsed" IsIndeterminate="True"/>-->
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Home View" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="26"></TextBlock>
            <TextBlock Text="{Binding Title}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1"/>
        </Grid>

    </Border>
</core:ContentPage>
